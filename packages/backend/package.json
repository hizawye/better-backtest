{
  "name": "better-backtest-backend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist --target bun",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "redis": "^4.6.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
